{{-- Style 2 File --}}

<section class="contact_hero_style2">
    <div class="contact_hero_style2-content global_container">
        <div class="contact_hero_style2-content-text">
            <h1>{{$title}}</h1>
            <p>{{$description}}</p>
        </div>
    </div>
    {{--
    
        <div class="contact_hero_style2-panel">
        <x-primary_button path="tel: {{$contact_number}}" text="{{$primary_action}}"></x-primary_button>
        <x-primary_button path="https://wa.me/{{$contact_whatsapp}}" text="{{$secondary_action}}"></x-primary_button>
    </div>
    --}}
</section>